package com.Backend.fullstackbackend.repository;

import com.Backend.fullstackbackend.model.TrainRadar;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TrainRadarRepository extends JpaRepository<TrainRadar, Long> {
}
